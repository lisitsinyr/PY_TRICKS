#------------------------------------------
# Strings_string01_method_find ():
#------------------------------------------
def Strings_string01_method_find ():
    """Strings_string01_method_find"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {Strings_string01_method_find.__name__}')
    print (f'#-----------------------------')

    # https://t.me/python_ready/659
    # 👩‍💻 Вспомним два полезных метода строк — find и replace
    #
    # Оба метода принимают подстроку, но find — находит и
    # возвращает индекс первого совпадения с подстрокой, а replace
    # — заменяет найденную подстроку на новую:
    #
    # • find — возвращает индекс найденного совпадения, принимает
    # необязательные аргументы — интервалы поиска и возвращает -1,
    # если ничего не найдет.
    #
    # • replace — возвращает копию исходной строки с новой
    # подстрокой вместо прежней.
    #
    # Metoaga replace()
    #
    # eoe0o
    # S.replace(old, new, count)
    #
    # eee
    # s = 'Bonbuue ropoga'
    # new_s = s.replace('ropoga', 'yymeca')
    #
    # print (new_s)
    #
    # = Pesynptat: ‘Bonbuiue uyfeca’
    #
    # Metog replace() BosBpaluaeT KonUIO UcxOgHON
    # CTPOKM, B KOTOPOM Bce nogCcTpOKN old 3amMeHeHbI Ha
    # nogcTpoKu new. Metog umeerT OBa o6A3aTeNbHbIXx
    # aprymMeuta - old u new, a Tak>Ke TpeTUN
    # Heo6azaTenbHbil - MAaKCUManbHOe KONUYeECTBO
    # 3aMeH, KOTOpOe 6yseT cHenaHo.

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Strings_string01_method_find ()
#endif

#endmodule
