#------------------------------------------
# Lists_list01_Expand ():
#------------------------------------------
def Lists_list01_Expand ():
    """Lists_list01_Expand"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {Lists_list01_Expand.__name__}')
    print (f'#-----------------------------')

    # https://t.me/python_easy_ru/1266
    # 🤔 За счет чего достигается константная величина ячейки
    # памяти для всех элементов, если типы данных в списке могут
    # быть разными?
    #
    # В Python список (list) — это массив указателей. Элементы
    # списка не хранятся внутри него, а находятся в других местах
    # памяти, а сам список содержит ссылки (указатели) на них.
    #
    # 🚩Как Python хранит список в памяти?
    #
    # Допустим, у нас есть список:
    # my_list = [10, "hello", 3.14, [1, 2, 3]]
    # +-----------------+
    # | Указатель → 10  |  (int)
    # | Указатель → "hello"  |  (str)
    # | Указатель → 3.14  |  (float)
    # | Указатель → [1, 2, 3] |  (list)
    # +-----------------+
    #
    # 🚩Проверим на практике
    #
    # Мы можем увидеть размер пустого списка и списка с элементами
    # с помощью sys.getsizeof():
    # import sys
    #
    # empty_list = []
    # print(sys.getsizeof(empty_list))  # 56 байт (сам список)
    #
    # filled_list = [10, "hello", 3.14, [1, 2, 3]]
    # print(sys.getsizeof(filled_list))  # 88 байт (список + 4
    # указателя)
    #
    # 🚩Размер ячейки списка всегда одинаковый
    #
    # Каждая ячейка списка занимает фиксированное количество
    # памяти, равное размеру указателя (8 байт на 64-битной
    # системе).
    # import ctypes
    #
    # my_list = [10, "hello", 3.14, [1, 2, 3]]
    #
    # for i in range(len(my_list)):
    #     print(ctypes.cast(id(my_list[i]),
    # ctypes.py_object).value)

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Lists_list01_Expand ()
#endif

#endmodule
