#------------------------------------------
# _01_ ():
#------------------------------------------
def _01_ ():
    """_01_"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {_01_.__name__}')
    print (f'#-----------------------------')

    # Link: https://t.me/c/2383487131/573
    #
    # Дата: 2025-06-16 16:00:15+00:00
    #
    # Title: Ghostly Python
    #
    # ⚙️ **Управление процессами на Python с помощью subprocess**
    #
    # `subprocess` — встроенный модуль Python для запуска внешних
    # программ, выполнения команд shell и взаимодействия с
    # процессами. Полезен при автоматизации, DevOps-скриптах и
    # интеграции с CLI-инструментами.
    #
    # ✔️ **Стандартная библиотека — установка не требуется**
    #
    # ⚙️ **Пример: выполнение shell-команды**
    #
    # ```import subprocess
    #
    # # Выполняем команду ls (или dir на Windows)
    # result = subprocess.run(["ls", "-l"], capture_output=True,
    # text=True)
    #
    # # Печатаем результат
    # print(result.stdout)```
    #
    # 💬 **Как это работает:**
    #
    # **1.** `subprocess.run([...])` запускает команду и ожидает
    # завершения.
    # **2.** `capture_output=True` сохраняет вывод команды.
    # **3.** `text=True` возвращает результат в виде строки (а не
    # байтов).
    #
    # **Отлично подходит для автоматизации сборки, запуска
    # скриптов, управления системными утилитами и бэкапами**

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    _01_ ()
#endif

#endmodule
