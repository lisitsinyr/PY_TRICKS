#------------------------------------------
# _01_ ():
#------------------------------------------
def _01_ ():
    """_01_"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {_01_.__name__}')
    print (f'#-----------------------------')

    # https://t.me/c/2383487131/500
    # 📦 Работа с архивами ZIP на Python с помощью zipfile
    #
    # Всё очень просто, если использовать встроенный модуль
    # zipfile. Это удобный способ создавать, читать и
    # распаковывать ZIP-архивы прямо в коде.
    #
    # ✔️ Установка не требуется — модуль входит в стандартную
    # библиотеку Python
    #
    # ⚙️ Пример кода
    #
    # import zipfile
    #
    # # Создаём архив
    # with zipfile.ZipFile("archive.zip", "w") as archive:
    #     archive.write("example.txt")
    #
    # # Распаковываем архив
    # with zipfile.ZipFile("archive.zip", "r") as archive:
    #     archive.extractall("extracted_files")
    #
    # 💬 Как это работает:
    #
    # 1. ZipFile("archive.zip", "w") создаёт новый архив для
    # записи файлов.
    # 2. write() добавляет файл в архив.
    # 3. extractall() извлекает все файлы из архива в указанную
    # папку.

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    _01_ ()
#endif

#endmodule
