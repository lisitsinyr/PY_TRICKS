#------------------------------------------
# _01_ ():
#------------------------------------------
def _01_ ():
    """_01_"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {_01_.__name__}')
    print (f'#-----------------------------')

    # https://t.me/pylinux0/829
    # 👩‍💻 Работа с базами данных SQLite в Python с использованием модуля sqlite3
    #
    # Работа с базами данных SQLite в Python с использованием модуля sqlite3 позволяет легко управлять данными, создавать таблицы, выполнять запросы и анализировать информацию.
    #
    # ➡️ Установка:
    #
    # Модуль sqlite3 встроен в стандартную библиотеку Python, дополнительная установка не требуется.
    #
    # ✔️ Простой пример:
    #
    # import sqlite3
    #
    # # Подключение к базе данных (или создание новой)
    # conn = sqlite3.connect('example.db')
    # cursor = conn.cursor()
    #
    # # Создание таблицы
    # cursor.execute('''
    # CREATE TABLE IF NOT EXISTS users (
    #     id INTEGER PRIMARY KEY,
    #     name TEXT,
    #     age INTEGER
    # )
    # ''')
    #
    # # Вставка данных
    # cursor.execute('''
    # INSERT INTO users (name, age) VALUES (?, ?)
    # ''', ('Alice', 30))
    #
    # # Сохранение изменений
    # conn.commit()
    #
    # # Получение данных
    # cursor.execute('SELECT * FROM users')
    # rows = cursor.fetchall()
    # for row in rows:
    #     print(row)
    #
    # # Закрытие соединения
    # conn.close()
    #
    # 🗣️ Этот скрипт создает базу данных SQLite, создает таблицу users, добавляет в неё запись и выводит все строки таблицы. Команда cursor.execute используется для выполнения SQL-запросов, а conn.commit сохраняет изменения.
    #
    # ⚙️ Обновление и удаление данных:
    #
    # SQLite позволяет легко обновлять и удалять данные из таблиц.
    #
    # # Обновление возраста пользователя с именем Alice
    # cursor.execute('''
    # UPDATE users SET age = ? WHERE name = ?
    # ''', (31, 'Alice'))
    # conn.commit()
    #
    # 🗣️ В этом примере возраст пользователя с именем Alice обновляется до 31 года. После выполнения запроса изменения сохраняются с помощью conn.commit.
    #
    # ➡️ Пример удаления данных:
    #
    # # Удаление пользователя с именем Alice
    # cursor.execute('''
    # DELETE FROM users WHERE name = ?
    # ''', ('Alice',))
    # conn.commit()
    #
    # 🗣️ Этот пример показывает, как удалить запись из таблицы по условию. В данном случае удаляется пользователь с именем Alice.
    #
    # ⚙️ Работа с параметризованными запросами:
    #
    # Для защиты от SQL-инъекций рекомендуется использовать параметризованные запросы.
    #
    # name = 'Bob'
    # cursor.execute('SELECT * FROM users WHERE name = ?', (name,))
    # rows = cursor.fetchall()
    #
    # 🗣️ Использование параметризованных запросов помогает избежать уязвимостей, связанных с SQL-инъекциями, передавая параметры запроса через кортеж или список.
    #
    # Работа с SQLite в Python предоставляет простой способ управления данными на локальном уровне. Это отличный инструмент для создания небольших приложений, анализа данных или хранения конфигурационной информации.

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    _01_ ()
#endif

#endmodule
