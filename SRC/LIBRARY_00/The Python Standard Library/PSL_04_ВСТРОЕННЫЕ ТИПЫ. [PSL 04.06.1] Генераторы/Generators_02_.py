#------------------------------------------
# Generators_02_ ():
#------------------------------------------
def Generators_02_ ():
    """Generators_02_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', Generators_02_.__name__)
    print ('#-----------------------------')

    # https://t.me/pythonl/4749
    # 🖥 Важная особенность генераторов в Python!
    #
    # Давай разберемся, как это работает:
    #
    # Что такое Генератор?
    # Функция my_generator_function является генератором, потому
    # что она использует ключевое слово yield.
    #
    # В отличие от обычной функции, которая выполняет весь код и
    # возвращает одно значение через return, генератор
    # "приостанавливается" на каждом yield, возвращая указанное
    # значение.
    #
    # При следующем вызове он возобновляет работу с того места,
    # где остановился.
    # Как работает yield:
    # Когда вы вызываете gen = my_generator_function(), код внутри
    # функции не выполняется.
    #
    # Создается специальный объект-генератор (gen).
    # Первый вызов next(gen) заставляет функцию выполниться до
    # первого yield 1. Функция возвращает 1 и приостанавливается.
    # Второй вызов next(gen) возобновляет выполнение с точки после
    # yield 1 и доходит до yield 2. Функция возвращает 2 и снова
    # приостанавливается.
    # Именно поэтому print(next(gen), next(gen)) выводит 1 2.
    #
    # Как работает return в генераторе:
    # Когда поток выполнения внутри генератора доходит до
    # оператора return (в нашем случае return 73) или просто до
    # конца функции без явного return, генератор считается
    # завершенным.
    #
    # Важно: Значение, указанное в return (здесь 73), не
    # возвращается как обычное значение через yield. Вместо этого
    # генератор выбрасывает (raises) специальное исключение:
    # StopIteration.
    #
    # Этот механизм StopIteration - стандартный способ в Python
    # сигнализировать, что итератор (а генератор - это тип
    # итератора) исчерпан.
    #
    # Перехват StopIteration и получение значения:
    # В правой части кода мы пытаемся вызвать next(gen) еще раз.
    #
    # Генератор возобновляется после yield 2, доходит до return 73
    # и выбрасывает StopIteration.
    #
    # Конструкция try...except StopIteration as err: перехватывает
    # это исключение.
    #
    # Ключевой момент (показан стрелкой на картинке): Значение,
    # которое было указано в операторе return генератора (73),
    # становится доступным как атрибут .value пойманного
    # исключения StopIteration.
    #
    # Поэтому print(err.value) выводит # 73.
    #
    # Итог:
    # Teturn в генераторе не производит очередное значение, а
    # завершает его работу. При этом значение из return
    # "упаковывается" в исключение StopIteration, сигнализирующее
    # об окончании, и его можно извлечь из атрибута .value этого
    # исключения, если перехватить его вручную.
    #
    # Стандартный цикл for item in generator(): в Python
    # автоматически обрабатывает StopIteration (просто завершает
    # цикл) и не дает прямого доступа к err.value. Поэтому для
    # демонстрации этого механизма и получения возвращаемого
    # значения используется явный вызов next() внутри блока
    # try...except.
    #
    #
    # BO3SBPALLL|AEMOE
    # 3HAYEHME FEHEPATOPA
    #
    # Korga reHepatop 3aBepluiaeT paborTy, OH MO>KeT
    # BePHYTb NPOUSBONbHOe 3HaYeHNe (Kak O6bI4HaA
    #
    # OYyHKyNa).
    # (a eco
    #
    # eee gen = my_generator_function()
    # def my_generator_function(): print (next(gen), next(gen) #12
    #
    # yield 1 try:
    #
    # yield 2 next(gen)
    #
    # return 73 except StopIteration as err:
    # Use code with caution print (err,value) #73
    #
    # 4 Use code with caution “A
    #
    # NPUKpenneHo K UCKMIOYeHNIO —----* 7
    # StopIteration, koTopoe f
    #
    # ‘
    #
    # BbIGpacbIBaeTCA Npn ero 3aBepLeHuUn.

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Generators_02_ ()
#endif

#endmodule
