#------------------------------------------
# _01_ ():
#------------------------------------------
def _01_ ():
    """_01_"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {_01_.__name__}')
    print (f'#-----------------------------')

    # https://t.me/zmeyipythonich/88
    # 🥰 Регулярные выражения
    #
    # Регулярные выражения  - это мощный инструмент для поиска и
    # обработки текста. В Python для работы с регулярными
    # выражениями используется модуль re. Вот основные моменты↩️
    #
    #
    # import re
    #
    #
    # re.search(pattern, string) — ищет первое совпадение с
    # шаблоном в строке.
    #
    #
    # re.match(pattern, string) — ищет совпадение только в начале
    # строки.
    #
    #
    # re.findall(pattern, string) — возвращает все совпадения в
    # виде списка.
    #
    #
    # re.sub(pattern, replacement, string) — заменяет все
    # совпадения на указанную строку.
    #
    # Основные символы и шаблоны↩️
    #
    # . — любой символ, кроме новой строки.
    #
    # \d — любая цифра (эквивалентно [0-9]).
    #
    # \D — любой символ, кроме цифры.
    #
    # \w — любая буква, цифра или нижнее подчеркивание
    # ([a-zA-Z0-9_]).
    #
    # \W — любой символ, кроме букв, цифр и нижнего подчеркивания.
    #
    # \s — любой пробельный символ (пробел, табуляция, новая
    # строка).
    #
    # \S — любой символ, кроме пробельных.
    #
    # [] — набор символов. Например, [a-z] — любая строчная буква.
    #
    # ^ — начало строки.
    #
    # $ — конец строки.
    #
    # * — 0 или более повторений.
    #
    # + — 1 или более повторений.
    #
    # ? — 0 или 1 повторение.
    #
    # {n} — ровно n повторений.
    #
    # {n, m} — от n до m повторений.
    #
    # Например, поиск совпадений↩️
    #
    #
    # import re
    #
    # text = "Мой телефон: 123-45-67, а другой: 987-65-43"
    #
    # # Ищем все номера телефонов в формате XXX-XX-XX
    # result = re.findall(r'\d{3}-\d{2}-\d{2}', text)
    # print(result)  # ['123-45-67', '987-65-43']
    #
    # ◾️Регулярные выражения нужно использовать аккуратно, чтобы
    # не усложнять код.

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    _01_ ()
#endif

#endmodule
