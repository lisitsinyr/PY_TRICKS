#------------------------------------------
# _01_ ():
#------------------------------------------
def _01_ ():
    """_01_"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {_01_.__name__}')
    print (f'#-----------------------------')
    # https://t.me/PythonTechCode/420
    # ✏️ Борьба с длинными строками
    #
    # Функция textwrap.fill() принимает текст в качестве входных
    # данных и возвращает отформатированный текст.
    #
    # Синтаксис:
    #
    # textwrap.fill(text, width=70, **kwargs)
    #
    # где:
    # text - строка, которую вы хотите отформатировать;
    #
    # width - максимальная ширина отформатированной строки. По
    # умолчанию 70 символов;
    #
    # **kwargs - дополнительные аргументы, такие как
    # initial_indent, subsequent_indent, expand_tabs,
    # replace_whitespace, и другие, которые могут управлять
    # поведением форматирования.
    #
    # Установка библиотеки:
    # pip install textwrap
    #
    # Этот инструмент позволяет удобно форматировать текст,
    # выравнивая его по заданной ширине и обрабатывая переносы
    # строк.
    # import textwrap
    #
    # for_text = textwrap.fill(text, width=40)
    #
    # A print(for_text)
    #
    # # 3TO ANUHHaA CTpOKa TeKCTa, kOTOpyl
    # # HeEOOXOAUMO OTHOPMaTMPOBaTb C NOMOLbIO
    # Pvt # textwrap.fill() gna ymo6OcTBa yTeHua.
    #
    # S77 / —=——- =~ — — <1
    #
    # a i

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    _01_ ()
#endif

#endmodule
