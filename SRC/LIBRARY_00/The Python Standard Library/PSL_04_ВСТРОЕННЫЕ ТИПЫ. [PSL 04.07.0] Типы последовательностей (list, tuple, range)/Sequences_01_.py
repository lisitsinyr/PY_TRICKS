#------------------------------------------
# Sequences_01_ ():
#------------------------------------------
def Sequences_01_ ():
    """Sequences_01_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', Sequences_01_.__name__)
    print ('#-----------------------------')

    # 🤔 Какие типы данных относятся к структурам данных?
    #
    # В Python существует множество структур данных, которые
    # предоставляют различные способы хранения и управления
    # данными. Они делятся на два основных типа: встроенные
    # структуры данных и пользовательские структуры данных
    # (созданные программистом). Встроенные структуры данных
    # предоставляют готовые инструменты для решения большинства
    # задач, а пользовательские разрабатываются вручную для более
    # сложных или специфичных случаев.
    #
    # 🚩Встроенные структуры данных
    #
    # К ним относятся те типы данных, которые изначально встроены
    # в Python. Они обеспечивают простое и удобное управление
    # данными. Вот основные типы:
    #
    # 🟠Список (List)
    # Массив, который может содержать элементы разных типов.
    # Динамический (размер меняется), упорядоченный (элементы
    # хранятся в порядке добавления).
    # my_list = [1, "hello", 3.14]
    # print(my_list[1])  # "hello"
    #
    # 🟠Кортеж (Tuple)
    # Похож на список, но неизменяемый. Используется для данных,
    # которые не должны быть изменены.
    # my_tuple = (10, 20, 30)
    # print(my_tuple[0])  # 10
    #
    # 🟠Множество (Set)
    # Неупорядоченная коллекция уникальных элементов. Удобно для
    # работы с множествами (поиск пересечений, объединений и
    # т.д.).
    # my_set = {1, 2, 3, 2}
    # print(my_set)  # {1, 2, 3}
    #
    # 🟠Словарь (Dictionary)
    # Хранит пары ключ-значение. Очень эффективен для быстрого
    # поиска данных по ключу.
    # my_dict = {"name": "Alice", "age": 25}
    # print(my_dict["name"])  # Alice
    #
    # 🚩Пользовательские структуры данных
    #
    # Эти структуры создаются с помощью классов или других
    # механизмов, доступных в Python. Они применяются для решения
    # задач, которые не могут быть эффективно выполнены
    # встроенными средствами.
    #
    # 🟠Стек (Stack)
    # Принцип работы: LIFO (последним пришел — первым ушел).
    # Реализуется через список или collections.deque.
    # stack = []
    # stack.append(10)  # Добавление
    # stack.append(20)
    # print(stack.pop())  # Удаление последнего элемента (20)
    #
    # 🟠Очередь (Queue)
    # Принцип работы: FIFO (первым пришел — первым ушел).
    # Реализуется через collections.deque или библиотеку queue.
    # from collections import deque
    #
    # queue = deque()
    # queue.append(10)
    # queue.append(20)
    # print(queue.popleft())  # 10
    #
    # 🟠Связный список (Linked List)
    # Элементы связаны друг с другом через указатели. Гибче
    # массивов, но сложнее в реализации.
    # class Node:
    #     def __init__(self, data):
    #         self.data = data
    #         self.next = None
    #
    # class LinkedList:
    #     def __init__(self):
    #         self.head = None
    #
    #     def append(self, data):
    #         if not self.head:
    #             self.head = Node(data)
    #         else:
    #             current = self.head
    #             while current.next:
    #                 current = current.next
    #             current.next = Node(data)

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Sequences_01_ ()
#endif

#endmodule
