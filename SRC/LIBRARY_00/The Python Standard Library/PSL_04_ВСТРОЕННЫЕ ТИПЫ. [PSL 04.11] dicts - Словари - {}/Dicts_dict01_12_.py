#------------------------------------------
# _01_ ():
#------------------------------------------
def _01_ ():
    """_01_"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {_01_.__name__}')
    print (f'#-----------------------------')

    # Link: https://t.me/c/1849804501/1141
    #
    # Дата: 2025-06-01 13:02:28+00:00
    #
    # Title: Программистика
    #
    # 👩‍💻 **Как НЕ надо и как НАДО использовать словари в Python**
    #
    # Словари (`dict`) — один из самых мощных инструментов в
    # Python. Но многие используют их неправильно или
    # неэффективно. Покажу частую ошибку и как её исправить.
    #
    #  ❌ ПЛОХО: грубая реализация
    # ```data = [('name', 'Alice'), ('age', 30), ('country',
    # 'USA')]
    # result = {}
    #
    # for item in data:
    #     key = item[0]
    #     value = item[1]
    #     result[key] = value
    #
    # print(result)```
    #
    # 📉 **Что не так:**
    #
    # 🟢Много лишнего кода
    # 🟢Не используется встроенный функционал Python
    # 🟢Нет обработки дубликатов или нестандартных ситуаций
    #
    # ✅ **ХОРОШО: лаконично и по-питоновски**
    # ```data = [('name', 'Alice'), ('age', 30), ('country',
    # 'USA')]
    # result = dict(data)
    #
    # print(result)```
    #
    # 📈 **Почему лучше:**
    #
    # 🟢 Коротко и понятно
    # 🟢 Используется встроенная функция `dict()`
    # 🟢 Меньше шансов на ошибку
    #
    #  🔥 **Если нужны значения по умолчанию**
    # ```from collections import defaultdict
    #
    # user_scores = defaultdict(int)
    #
    # user_scores['Alice'] += 10
    # user_scores['Bob'] += 5
    #
    # print(user_scores)  # {'Alice': 10, 'Bob': 5}```
    #
    # 📌 `defaultdict` автоматически создаёт значение, если ключа
    # ещё нет. Удобно для счётчиков, группировок и т.п.
    #
    #  🧠 Запомни:
    #
    # 🟢 Используй `dict()` для создания словарей из пар
    # 🟢 Не изобретай велосипед — используй `defaultdict` и
    # `dict.get()`
    # 🟢 Чистый код = меньше багов и больше удовольствия от Python
    # ❤️

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    _01_ ()
#endif

#endmodule
