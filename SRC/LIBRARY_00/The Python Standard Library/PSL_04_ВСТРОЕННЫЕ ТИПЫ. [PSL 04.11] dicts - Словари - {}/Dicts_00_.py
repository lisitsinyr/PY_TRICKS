#------------------------------------------
# Dicts_00_ ():
#------------------------------------------
def Dicts_00_ ():
    """Dicts_00_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', Dicts_00_.__name__)
    print ('#-----------------------------')

    # Обзор от ИИ
    # Подробнее…
    # В программировании, особенно в языках типа Python, set и dict — это два основных типа данных, используемые для хранения коллекций. Set представляет собой неупорядоченную коллекцию уникальных элементов, а dict (или словарь) — это неупорядоченная коллекция пар "ключ-значение".
    # Основные различия:
    # Тип данных:
    # set — это коллекция элементов, а dict — это коллекция пар "ключ-значение".
    # Упорядоченность:
    # Оба типа данных обычно неупорядочены, что означает, что порядок элементов или пар "ключ-значение" не гарантируется.
    # Уникальность:
    # set гарантирует, что элементы в нем уникальны, то есть не может быть одинаковых элементов. dict гарантирует уникальность ключей, но значения могут быть одинаковыми.
    # Доступ к элементам:
    # В set нет индексации, поэтому доступ к элементам можно получить только через перебор, например, с помощью цикла. В dict доступ к значениям происходит по ключам.
    # Использование:
    # set часто используется для проверки наличия элементов в коллекции, выполнения операций над коллекциями (объединение, пересечение), и дедупликации данных. dict используется для хранения данных, связанных между собой, где ключ помогает идентифицировать значение.
    # Пример:
    # Python
    #
    # # set
    # my_set = {1, 2, 3, 4, 4}  # Дубликат 4 будет автоматически убран
    # print(my_set)  # Вывод: {1, 2, 3, 4}
    #
    # # dict
    # my_dict = {"apple": 1, "banana": 2, "orange": 3}
    # print(my_dict)  # Вывод: {'apple': 1, 'banana': 2, 'orange': 3}
    #
    # # Доступ к элементам
    # print(my_dict["banana"])  # Вывод: 2 (доступ по ключу "banana")
    # Вкратце:
    # set — для хранения уникальных элементов, без дубликатов.
    # dict — для хранения связанных данных в виде пар "ключ-значение".

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Dicts_00_ ()
#endif

#endmodule
