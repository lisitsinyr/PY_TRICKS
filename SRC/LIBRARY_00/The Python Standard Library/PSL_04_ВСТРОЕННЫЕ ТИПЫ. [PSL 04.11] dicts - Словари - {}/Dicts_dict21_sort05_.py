#------------------------------------------
# Dicts_dict21_sort02 ():
#------------------------------------------
def Dicts_dict21_sort02():
    """Dicts_dict21_sort02"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {Dicts_dict21_sort02.__name__}')
    print (f'#-----------------------------')

    # Link: https://t.me/python_easy_ru/1415
    #
    # Дата: 2025-05-26 09:05:15+00:00
    #
    # Title: Python | Вопросы собесов
    #
    # **🤔**** Как отсортировать список словарей по определенному
    # полю?**
    #
    # Для сортировки списка словарей по определенному полю в
    # Python удобно использовать функцию sorted() или метод
    # sort(). Оба подхода позволяют указать ключ сортировки с
    # помощью параметра key, где можно передать либо функцию, либо
    # лямбда-выражение, которое извлекает значение из словаря для
    # сортировки.
    #
    # **🚩****Почему это важно?**
    #
    # Списки словарей часто используются для хранения
    # структурированных данных. Например, вы можете иметь список
    # сотрудников, где каждый сотрудник представлен в виде словаря
    # с полями, такими как имя, возраст и зарплата. Сортировка по
    # определенному полю позволяет упорядочить данные, чтобы ими
    # было проще пользоваться или отображать.
    # **
    # ****🚩****Как это сделать?
    # **
    # 🟠**Использование функции `sorted()`**
    # Эта функция возвращает новый отсортированный список.
    #
    # ```   employees = [
    #        {"name": "Alice", "age": 30, "salary": 70000},
    #        {"name": "Bob", "age": 25, "salary": 50000},
    #        {"name": "Charlie", "age": 35, "salary": 120000}
    #    ]
    #
    #    # Сортировка по возрасту
    #    sorted_employees = sorted(employees, key=lambda x:
    # x["age"])
    #
    #    print(sorted_employees)
    #    ```
    #
    #    Результат
    #
    # ```   [{'name': 'Bob', 'age': 25, 'salary': 50000},
    #     {'name': 'Alice', 'age': 30, 'salary': 70000},
    #     {'name': 'Charlie', 'age': 35, 'salary': 120000}]
    #    ```
    #
    # 🟠**Использование метода `sort()`**
    # Этот метод изменяет существующий список.
    #
    # ```   employees = [
    #        {"name": "Alice", "age": 30, "salary": 70000},
    #        {"name": "Bob", "age": 25, "salary": 50000},
    #        {"name": "Charlie", "age": 35, "salary": 120000}
    #    ]
    #
    #    # Сортировка по зарплате
    #    employees.sort(key=lambda x: x["salary"])
    #
    #    print(employees)
    #    ```
    #
    #    Результат
    #
    # ```   [{'name': 'Bob', 'age': 25, 'salary': 50000},
    #     {'name': 'Alice', 'age': 30, 'salary': 70000},
    #     {'name': 'Charlie', 'age': 35, 'salary': 120000}]
    #    ```
    #
    # 🟠**Сортировка в обратном порядке**
    # Установите параметр reverse=True, чтобы отсортировать в
    # порядке убывания.
    #
    # ```   sorted_employees_desc = sorted(employees, key=lambda
    # x: x["age"], reverse=True)
    #    print(sorted_employees_desc)
    #    ```
    #
    # 🟠**Использование функции `itemgetter` из модуля `operator`**
    # Это более эффективный способ, чем лямбда-функция, особенно
    # для больших данных.
    #
    # ```   from operator import itemgetter
    #
    #    sorted_employees = sorted(employees,
    # key=itemgetter("age"))
    #    print(sorted_employees)
    #    ```
    #
    # 🚩**Обработка отсутствующих значений**
    #
    # Если поле может отсутствовать в некоторых словарях, можно
    # использовать параметр key для обработки таких ситуаций.
    # ```employees = [
    #     {"name": "Alice", "age": 30},
    #     {"name": "Bob"},
    #     {"name": "Charlie", "age": 35}
    # ]
    #
    # sorted_employees = sorted(employees, key=lambda x:
    # x.get("age", 0))
    # print(sorted_employees)```
    #
    # Результат
    # ```[{'name': 'Bob'},
    #  {'name': 'Alice', 'age': 30},
    #  {'name': 'Charlie', 'age': 35}]```
    
#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Dicts_dict21_sort02 ()
#endif

#endmodule
