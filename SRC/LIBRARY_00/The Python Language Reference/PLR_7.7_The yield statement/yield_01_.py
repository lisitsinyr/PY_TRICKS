#------------------------------------------
# yield_01_ ():
#------------------------------------------
def yield_01_ ():
    """yield_01_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', yield_01_.__name__)
    print ('#-----------------------------')

    # Link: https://t.me/pytstart/270
    #
    # Дата: 2025-06-06 10:03:08+00:00
    #
    # Title: PytStart | Программирование на Python
    #
    # 🔁** Как работает** `yield`** и зачем он реально нужен в
    # Python**
    #
    # `yield` — это не `return`. Он не завершает функцию, а
    # **паузит** её и **отдаёт результат по частям**.
    #
    #  ✅ **Простой пример: отдаём по одному**
    # ```
    # def gen():
    #     yield 1
    #     yield 2
    #     yield 3
    #
    # for x in gen():
    #     print(x)```
    # 👉 Вывод: `1`, `2`, `3` — не сразу, а **по одному**.
    # Функция работает **до первого `yield`**, потом «засыпает» и
    # продолжает дальше.
    #
    #  🧾 **Чтение большого файла без нагрузки**
    # ```
    # def read_lines(path):
    #     with open(path) as f:
    #         for line in f:
    #             yield line.strip()
    #
    # for line in read_lines("huge.log"):
    #     print(line)```
    # Если файл весит 10 ГБ — всё равно работает.
    # Никаких `readlines()`, которые жрут память.
    #
    #  🔢** Бесконечный счётчик**
    # ```
    # def counter():
    #     n = 0
    #     while True:
    #         yield n
    #         n += 1
    #
    # for i in counter():
    #     if i > 5:
    #         break
    #     print(i)```
    # 👉 Без `yield` тут не обойтись.
    # Можно генерировать **бесконечный поток**, но обрабатывать
    # его как обычный список.
    #
    #  📤 **Свой генератор фильтра**
    # ```
    # def even_only(numbers):
    #     for n in numbers:
    #         if n % 2 == 0:
    #             yield n
    #
    # nums = [1, 2, 3, 4, 5, 6]
    # for x in even_only(nums):
    #     print(x)```
    # 👉 Не `filter()`, а свой контролируемый фильтр. Гибко и
    # понятно.
    #
    # 🗣️** Запомни:**`yield` — это когда ты **генерируешь данные
    # на лету**.
    # Ты не ждёшь весь список, не держишь всё в памяти — просто
    # **отдаёшь, когда нужно**.
    # Нужен потоковый режим? `yield` — лучший друг.


#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    yield_01_ ()
#endif

#endmodule
