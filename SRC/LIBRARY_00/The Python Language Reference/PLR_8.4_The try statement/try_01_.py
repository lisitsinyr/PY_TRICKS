#------------------------------------------
# try_01_ ():
#------------------------------------------
def try_01_ ():
    """try_01_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', try_01_.__name__)
    print ('#-----------------------------')

    # https://t.me/python_easy_ru/1204
    # 🤔 Для чего могут применять конструкцию try finally без
    # except?
    #
    # Конструкция try...finally используется в случаях, когда
    # нужно гарантировать выполнение кода в finally, независимо от
    # того, возникло исключение или нет.
    #
    # 🟠Закрытие файла
    # Если файл открыт, его нужно закрыть в любом случае, даже
    # если в процессе работы произойдёт ошибка.
    # try:
    #     file = open("data.txt", "r")
    #     data = file.read()
    # finally:
    #     print("Закрываем файл...")
    #     file.close()  # Файл закроется даже при ошибке
    #
    # 🟠Освобождение ресурсов (например, соединение с базой данных)
    # Если программа работает с базой данных, соединение нужно
    # закрыть, даже если произошла ошибка.
    # import sqlite3
    #
    # conn = sqlite3.connect("database.db")
    #
    # try:
    #     cursor = conn.cursor()
    #     cursor.execute("SELECT * FROM users")  # Ошибка, если
    # таблицы нет
    # finally:
    #     print("Закрываем соединение с БД...")
    #     conn.close()  # Закроет соединение в любом случае
    #
    # 🟠Разблокировка ресурсов (например, файл или поток)
    # Допустим, есть блокировка файла, которую нужно снять в любом
    # случае.
    # import threading
    #
    # lock = threading.Lock()
    #
    # try:
    #     lock.acquire()
    #     print("Ресурс заблокирован")
    #     # Код, который использует ресурс
    # finally:
    #     print("Разблокируем ресурс")
    #     lock.release()  # Освободит блокировку даже при ошибке
    #
    # Пример 4: Остановка таймера, даже если произошла ошибка
    # import time
    #
    # try:
    #     start_time = time.time()
    #     x = 1 / 0  # Ошибка деления на ноль
    # finally:
    #     elapsed_time = time.time() - start_time
    #     print(f"Программа выполнялась {elapsed_time:.2f}
    # секунд")

#endfunction


#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    try_01_ ()
#endif

#endmodule
