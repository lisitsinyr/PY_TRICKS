#------------------------------------------
# match_01_ ():
#------------------------------------------
def match_01_ ():
    """match_01_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', match_01_.__name__)
    print ('#-----------------------------')

    # https://t.me/pytstart/128
    # 🧬 Структурные шаблоны (match/case) с объектами – не только
    # для строк и чисел!
    #
    # Начиная с Python 3.10, match/case позволяет не просто
    # сравнивать, но разбирать структуры объектов.
    #
    # 📌 Пример с классами:
    #
    # class Point:
    #     def __init__(self, x, y): self.x, self.y = x, y
    #
    # def handle(p):
    #     match p:
    #         case Point(x=0, y=y):
    #             print(f"На оси Y: y={y}")
    #         case Point(x=x, y=0):
    #             print(f"На оси X: x={x}")
    #         case Point():
    #             print("Внутри плоскости")
    #
    # handle(Point(0, 5))
    #
    # Работает благодаря __match_args__ — специальному атрибуту:
    # class Point:
    #     __match_args__ = ("x", "y")
    #
    # 🗣️ Возможности match/case гораздо шире if — особенно при
    # парсинге AST, JSON, конфигураций и DSL.

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    match_01_ ()
#endif

#endmodule
