#------------------------------------------
# Decorators_04_ ():
#------------------------------------------
def Decorators_04_ ():
    """Decorators_04_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', Decorators_04_.__name__)
    print ('#-----------------------------')

    # https://t.me/python_easy_ru/1239
    # 🤔 Как на уровне Python реализован декоратор?
    #
    # В Python декораторы — это функции, которые оборачивают
    # другие функции. Они добавляют или изменяют поведение функции
    # без изменения её кода.
    #
    # 🟠Реализация простого декоратора вручную
    # Простейший декоратор, который добавляет текст перед
    # выполнением функции
    # def my_decorator(func):
    #     def wrapper():
    #         print("Декоратор сработал!")
    #         return func()  # Вызываем исходную функцию
    #     return wrapper  # Возвращаем обёрнутую функцию
    #
    # def say_hello():
    #     print("Hello!")
    #
    # say_hello = my_decorator(say_hello)  # Оборачиваем вручную
    # say_hello()
    #
    # Вывод
    # Декоратор сработал!
    # Hello!
    #
    # 🟠Реализация декоратора через `@`
    # Python позволяет упрощённый синтаксис через @
    # def my_decorator(func):
    #     def wrapper():
    #         print("Декоратор сработал!")
    #         return func()
    #     return wrapper
    #
    # @my_decorator  # Эквивалентно say_hello =
    # my_decorator(say_hello)
    # def say_hello():
    #     print("Hello!")
    #
    # say_hello()
    #
    # 🟠Декоратор с `args` и `kwargs` (универсальный вариант)
    # Если функция принимает аргументы, их нужно передавать через
    # args и kwargs
    # def my_decorator(func):
    #     def wrapper(*args, **kwargs):  # Поддержка любых
    # аргументов
    #         print(f"Вызываем {func.__name__} с аргументами:
    # {args}, {kwargs}")
    #         return func(*args, **kwargs)  # Вызываем
    # оригинальную функцию
    #     return wrapper
    #
    # @my_decorator
    # def greet(name):
    #     print(f"Привет, {name}!")
    #
    # greet("Alice")
    #
    # Вывод
    # Вызываем greet с аргументами: ('Alice',), {}
    # Привет, Alice!
    #
    # 🟠Декоратор с параметрами (фабрика декораторов)
    # Чтобы передавать параметры в декоратор, создаём функцию,
    # которая возвращает декоратор
    # def repeat(times):  # Функция с параметром
    #     def decorator(func):
    #         def wrapper(*args, **kwargs):
    #             for _ in range(times):  # Повторяем вызов
    # функции
    #                 func(*args, **kwargs)
    #         return wrapper
    #     return decorator  # Возвращаем декоратор
    #
    # @repeat(3)  # Передаём 3 → `repeat(3)` вернёт `decorator`
    # def hello():
    #     print("Hello!")
    #
    # hello()
    #
    # Вывод
    # Hello!
    # Hello!
    # Hello!
    #
    # 🟠Декораторы классов (`functools.wraps`)
    # Обычные декораторы ломают метаданные функции (__name__,
    # __doc__).
    # import functools
    #
    # def my_decorator(func):
    #     @functools.wraps(func)  # Сохраняем метаданные
    #     def wrapper(*args, **kwargs):
    #         print("Декоратор сработал!")
    #         return func(*args, **kwargs)
    #     return wrapper
    #
    # @my_decorator
    # def greet():
    #     """Функция приветствия"""
    #     print("Hello!")
    #
    # print(greet.__name__)  # greet (без wraps было бы wrapper)
    # print(greet.__doc__)  # Функция приветствия
    
#endfunction


#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Decorators_04_ ()
#endif

#endmodule
