#------------------------------------------
# Functions_14_ ():
#------------------------------------------
def Functions_14_ ():
    """Functions_14_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', Functions_14_.__name__)
    print ('#-----------------------------')

    # https://t.me/pythonercode/1691
    # ✈️Сегодня поговорим о настоящей магии в мире Python –
    # метапрограммировании и динамическом создании функций.
    #
    # ➡️Начнем с простого примера. Помните eval() и exec()? Это
    # базовые инструменты метапрограммирования, но использовать их
    # нужно с осторожностью – как острый нож в руках повара.
    # Гораздо интереснее копнуть глубже!
    #
    # def create_power_function(power):
    #     def power_func(x):
    #         return x ** power
    #     return power_func
    #
    # # Создаём функции на лету
    # square = create_power_function(2)
    # cube = create_power_function(3)
    #
    # ⬆️Но это только верхушка айсберга! Настоящее веселье
    # начинается, когда мы подключаем модуль types и функцию exec
    # с собственным словарем namespace. Это как собирать
    # конструктор, только вместо деталей – части функции.
    #
    # 🔎А вы знали, что можно создавать функции с динамическим
    # количеством аргументов? Или генерировать методы классов на
    # лету? Это как будто у вас есть 3D-принтер для кода!
    #
    # import types
    #
    # def create_dynamic_function(func_name, args, body):
    #     namespace = {}
    #     func_code = f"def {func_name}({',
    # '.join(args)}):\n{body}"
    #     exec(func_code, globals(), namespace)
    #     return namespace[func_name]
    #
    # # Магия в действии
    # greet = create_dynamic_function(
    #     "greet",
    #     ["name"],
    #     "    return f'Привет, {name}!'"
    # )
    #
    # ➡️Но помните – с большой силой приходит большая
    # ответственность! Метапрограммирование может сделать код
    # сложным для понимания. Используйте его мудро, когда выгода
    # очевидна:
    # —При создании API с повторяющимися паттернами
    # —Для автоматизации рутинных задач
    # —При разработке фреймворков и библиотек
    #
    # ➡️Отдельного внимания заслуживают декораторы – это ведь тоже
    # метапрограммирование! Они как умные обёртки для функций,
    # которые могут изменять их поведение. И да, их тоже можно
    # создавать динамически!


#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Functions_14_ ()
#endif

#endmodule
