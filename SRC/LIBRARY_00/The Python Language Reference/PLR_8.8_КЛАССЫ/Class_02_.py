#------------------------------------------
# Class_02_ ():
#------------------------------------------
def Class_02_ ():
    """Class_02_"""
#beginfunction
    print ('#-----------------------------')
    print ('#', Class_02_.__name__)
    print ('#-----------------------------')

    # https://t.me/pytstart/217
    # 👩‍💻 `@property`: не просто синтаксический сахар
    #
    # В Python у тебя есть мощный способ скрыть логику за обычным
    # доступом к атрибутам — и имя ему @property.
    #
    # Пример:
    #
    # class Circle:
    #     def __init__(self, radius):
    #         self._radius = radius
    #
    #     @property
    #     def area(self):
    #         return math.pi * self._radius ** 2
    #
    # Теперь:
    #
    # c = Circle(3)
    # print(c.area)  # 28.27...
    #
    # ➡️ area выглядит как поле, но за ним — код. Удобно,
    # безопасно и читаемо.
    #
    # 🔥 Где полезно использовать `property`:
    #
    # 1. Когда значение вычисляется "на лету"
    #
    # @property
    # def is_active(self):
    #     return self.last_seen > datetime.now() -
    # timedelta(minutes=5)
    #
    # ➡️  user.is_active читается как флаг, но всегда актуален.
    #
    # 2. Когда хочешь валидировать данные при установке
    #
    # @property
    # def age(self):
    #     return self._age
    #
    # @age.setter
    # def age(self, value):
    #     if value < 0:
    #         raise ValueError("Возраст не может быть
    # отрицательным")
    #     self._age = value
    #
    # ➡️  Можно написать person.age = -1 — но это вызовет ошибку,
    # как и должно.
    #
    # 3. Когда хочешь спрятать внутренности, не меняя интерфейс
    #
    # Сначала у тебя было:
    #
    # user.is_admin  # булево поле
    #
    # А потом ты передумал и решил вычислять его по ролям:
    #
    # @property
    # def is_admin(self):
    #     return "admin" in self.roles
    #
    # ➡️  Все старые вызовы user.is_admin останутся рабочими, но
    # теперь они динамичные.
    #
    #
    # 🧠 @property делает твой код гибким: интерфейс остаётся, а
    # реализацию можно менять. Это инкапсуляция, но по-питоновски.
    #
    # Ты можешь добавить к setter'у ещё и deleter:
    #
    # @x.deleter
    # def x(self):
    #     del self._x
    #
    # Но чаще всего нужны только getter и setter.
    #
    # 🗣️ Запомни: @property — это не украшение. Это способ скрыть
    # логику, проверки, кэширование, не портя интерфейс.
    # Ты получаешь читаемый API, не жертвуя гибкостью.
    #
    #
    # 📍Где применяется:
    # ORM, модели, бизнес-логика, интерфейсы SDK, динамические
    # флаги, защитные проверки. Везде, где важно: "пусть выглядит
    # просто, но работает как надо".

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Class_02_ ()
#endif

#endmodule
