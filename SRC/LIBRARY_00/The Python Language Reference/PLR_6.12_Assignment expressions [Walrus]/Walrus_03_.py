#------------------------------------------
# Operators_operator01_Walrus_03 ():
#------------------------------------------
def Operators_operator01_Walrus_03 ():
    """Operators_operator01_Walrus_03"""
#beginfunction
    print (f'#-----------------------------')
    print (f'# {Operators_operator01_Walrus_03.__name__}')
    print (f'#-----------------------------')

    # 🤔 Что такое моржовый оператор?
    #
    # Моржовый оператор (:=) – это новый оператор, появившийся в
    # Python 3.8, который позволяет присваивать значение
    # переменной прямо внутри выражения.
    #
    # 🚩Как он работает?
    #
    # Обычно мы записываем код так:
    # value = len(my_list)  # Сначала присваиваем
    # if value > 10:  # Потом используем
    #     print("Список большой")
    #
    # С := можно совместить оба действия
    # if (value := len(my_list)) > 10:
    #     print("Список большой")
    #
    # 🚩Где использовать?
    #
    # В циклах (избегаем лишних вычислений). Вместо:
    # data = input("Введите строку: ")
    # while data != "exit":
    #     print("Вы ввели:", data)
    #     data = input("Введите строку: ")
    #
    # С := можно записать короче:
    # while (data := input("Введите строку: ")) != "exit":
    #     print("Вы ввели:", data)
    #
    # В if и while (проверяем и присваиваем одновременно)
    # Без :=
    # text = input("Введите слово: ")
    # if len(text) > 5:
    #     print(f"Слово длинное ({len(text)} символов)")
    #
    # С :=:
    # if (length := len(text)) > 5:
    #     print(f"Слово длинное ({length} символов)")
    #
    # В списковых включениях (list comprehensions)
    # Без :=:
    # numbers = [random.randint(1, 100) for _ in range(10)]
    # filtered = [num for num in numbers if num % 2 == 0]
    #
    # С :=:
    # filtered = [num for _ in range(10) if (num :=
    # random.randint(1, 100)) % 2 == 0]
    #
    # 🚩Когда не стоит использовать `:=`?
    #
    # Если код становится сложнее для чтения
    # if (a := func()) and (b := another_func(a)) > 10:
    #     ...


    # 🐍Оператор моржа в Python  := 🦭
    # В программировании, оператор моржа в Python, также известный
    # как оператор присваивания с условием, является нововведением
    # в версии Python 3.8. Он предоставляет удобный способ
    # проверить условие и присвоить значение переменной, если
    # условие истинно.
    #
    # Оператор моржа в Python обозначается  двумя символами ":= ",
    # за которыми следует символ ">" для сравнения. Этот оператор
    # может быть использован в выражениях if и while, чтобы
    # одновременно проверить условие и присвоить значение
    # переменной. Например:
    #
    # if (x := some_function()) > 10:
    #     print("x is greater than 10")
    # else:
    #     print("x is less than or equal to 10")
    #
    #
    # В этом примере, если значение, возвращаемое функцией
    # some_function(), больше 10, то оно будет присвоено
    # переменной x, и будет выведено сообщение "x is greater than
    # 10". В противном случае будет выведено сообщение "x is less
    # than or equal to 10".
    #
    # ➕ Преимущества использования оператора моржа
    #
    # Оператор моржа предлагает несколько преимуществ, которые
    # могут значительно упростить кодирование. Во-первых, он
    # позволяет объединить проверку условия и присваивание
    # значения в одной строке, что делает код более компактным и
    # легким для чтения. Во-вторых, он устраняет необходимость
    # использования временных переменных для хранения результатов
    # выражений, что уменьшает количество кода и повышает
    # эффективность. Наконец, оператор моржа может быть
    # использован в циклах, что позволяет более эффективно
    # использовать ресурсы и улучшить производительность
    # программы.



    # 🤔 Что такое моржовый оператор?
    #
    # Моржовый оператор (:=) – это новый оператор, появившийся в
    # Python 3.8, который позволяет присваивать значение
    # переменной прямо внутри выражения.
    #
    # 🚩Как он работает?
    #
    # Обычно мы записываем код так:
    # value = len(my_list)  # Сначала присваиваем
    # if value > 10:  # Потом используем
    #     print("Список большой")
    #
    # С := можно совместить оба действия
    # if (value := len(my_list)) > 10:
    #     print("Список большой")
    #
    # 🚩Где использовать?
    #
    # В циклах (избегаем лишних вычислений). Вместо:
    # data = input("Введите строку: ")
    # while data != "exit":
    #     print("Вы ввели:", data)
    #     data = input("Введите строку: ")
    #
    # С := можно записать короче:
    # while (data := input("Введите строку: ")) != "exit":
    #     print("Вы ввели:", data)
    #
    # В if и while (проверяем и присваиваем одновременно)
    # Без :=
    # text = input("Введите слово: ")
    # if len(text) > 5:
    #     print(f"Слово длинное ({len(text)} символов)")
    #
    # С :=:
    # if (length := len(text)) > 5:
    #     print(f"Слово длинное ({length} символов)")
    #
    # В списковых включениях (list comprehensions)
    # Без :=:
    # numbers = [random.randint(1, 100) for _ in range(10)]
    # filtered = [num for num in numbers if num % 2 == 0]
    #
    # С :=:
    # filtered = [num for _ in range(10) if (num :=
    # random.randint(1, 100)) % 2 == 0]
    #
    # 🚩Когда не стоит использовать `:=`?
    #
    # Если код становится сложнее для чтения
    # if (a := func()) and (b := another_func(a)) > 10:
    #     ...

#endfunction

#------------------------------------------
#
#------------------------------------------
#beginmodule
if __name__ == "__main__":
    Operators_operator01_Walrus_03 ()
#endif

#endmodule
